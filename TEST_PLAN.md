# v0.4.0 Chrome / Edge 上架前测试

这份清单用于在真实的全新浏览器配置文件中验证功能。测试时建议使用演示书签，不要使用真实账号、密码或工作书签。

## 准备

1. 分别打开全新的 Chrome 配置文件和 Edge 配置文件。
2. 在 `chrome://extensions` 或 `edge://extensions` 开启开发者模式，选择“加载已解压的扩展程序”，指向本项目目录。
3. 在来源配置文件建立演示结构：空书签栏、一个普通书签、一个同名文件夹、一个包含 50 个以上书签的子文件夹。

## 功能用例

- [ ] 发送位置：默认显示顶层和一级子文件夹；点击“浏览全部子文件夹”或输入关键词后，匹配列表和数量正确更新。
- [ ] 无密码发送：生成同步码，目标端直接预览并合并。
- [ ] 有密码发送：使用 8 位以上密码；错误密码必须提示失败，正确密码才显示预览。
- [ ] 确认密码：发送端两次密码不一致时禁止生成。
- [ ] 文件传递：保存 `.bookmarkbridge` 文件，在另一配置文件点击“从文件导入”。同步码较长时“保存为文件”应有突出提示。
- [ ] 侧边栏导入：点击“在侧边栏选择文件”，确认文件选择发生在书签桥侧边栏；返回并重新打开 Popup 后文件自动载入，载入一次后不会重复恢复。
- [ ] 空文件夹：发送空文件夹，目标端逐条预览并能正确创建文件夹。
- [ ] 重复书签：重复导入同一同步码，智能合并应显示“跳过重复”而不重复创建。
- [ ] 多次合并历史：连续合并两组同步码，分别点击两条历史记录的“撤销”，确认只删除对应批次。
- [ ] 大量书签：发送 500 个以上书签，观察进度提示、逐条差异列表和文件导入可用性。
- [ ] 取消生成/预览/合并/撤销：在进度条出现时点击“取消”，确认界面恢复可操作；部分合并应出现在历史中并可撤销。
- [ ] 损坏同步码：删除字符、粘贴普通文本、导入空文件，必须给出错误提示且不修改目标书签。
- [ ] 弹窗重开：关闭并重新打开扩展，确认不会自动恢复上一次同步码或旧的接收内容。

## Chrome / Edge 交叉测试

- [ ] Chrome 配置文件生成 → Edge 配置文件文件导入 → 预览 → 合并。
- [ ] Edge 配置文件生成 → Chrome 配置文件文件导入 → 预览 → 合并。
- [ ] Chrome 和 Edge 各自重复导入一次，确认去重逻辑一致。
- [ ] 两个浏览器都显示正确的浏览器名称、图标和 v0.4 版本。

## 发布前记录

测试通过后记录浏览器版本、操作系统、测试日期和失败用例。不要把真实同步码、密码或真实书签内容提交到仓库或截图中。
