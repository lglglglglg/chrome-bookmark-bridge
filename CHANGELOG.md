# 更新日志

## 0.4.0 - 2026-09-19

- 将数量差异预览升级为逐条书签和文件夹差异，并限制界面首屏显示前 200 条，避免超大书签树卡顿。
- 支持同一弹窗会话内保留多次合并历史，可分别撤销任意一次合并。
- 发送、预览、合并和撤销增加取消操作；取消中的部分合并也会进入历史，便于回滚。
- 超长同步码会突出“保存为文件”按钮，并提示目标端使用文件导入。

## 0.3.2 - 2026-09-19

- 修复重复点击“复制同步码”时状态提示不断叠加的问题。

## 0.3.1 - 2026-09-19

- 修复 Chrome/Edge 小书签同步在压缩流阶段长时间等待的问题。
- 密码改为可选：留空可生成未加密同步码，填写密码才启用 AES-GCM。
- 不再自动恢复上一次同步码，避免接收端误操作旧数据。
- 根据当前浏览器显示 Chrome 或 Edge，顶部改用书签桥图标。
- 移除不再使用的扩展本地存储权限。

## 0.3.0 - 2026-09-19

- 增加发送端同步密码二次确认。
- 增加目标端差异预览：预计新增文件夹、书签和跳过的重复项。
- 增加“撤销本次合并”，只删除本次操作创建的节点。
- 发送端支持选择任意子文件夹。
- 同步内容优先使用 gzip 压缩，并显示加密、解密和合并进度。
- 保留 BM1 同步码兼容读取。

## 0.2.0 - 2026-09-19

- 增加 AES-GCM 加密同步码。
- 增加 `.bookmarkbridge` 文件导入/导出。
- 增加扩展图标和 Chrome Web Store 发布资料。
