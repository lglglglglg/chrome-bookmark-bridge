# 书签桥隐私政策

生效日期：2026-09-19

书签桥（以下简称“本扩展”）尊重你的隐私。本政策说明本扩展如何处理信息。

## 我们处理什么信息

本扩展只在你主动点击相关按钮时读取当前 Chrome 配置文件中的书签，用于生成书签快照、预览和合并。书签内容可能包含网址、标题和文件夹名称。

本扩展不会读取或处理 Gmail、Google 账号凭据、密码、浏览历史、网页内容或其他 Chrome 配置文件的数据。

## 数据是否上传

不会。本扩展没有远程服务器、分析服务、广告 SDK 或账号系统。书签快照只在本地生成，并使用你设置的密码通过 Web Crypto AES-GCM 加密。加密同步码由你自行复制、粘贴或保存为文件；本扩展不会接收或上传它。

为方便你继续操作，最近一次同步码可能暂存在 Chrome 扩展本地存储中，且仅保留在当前 Chrome 配置文件。你可以通过清除扩展数据删除它。

## 权限用途

- `bookmarks`：读取和创建你明确选择的书签。
- `storage`：暂存最近一次同步码，避免弹窗关闭后丢失。
- `clipboardWrite`：在你点击“复制同步码”时写入剪贴板。

本扩展不申请读取剪贴板、网络访问、标签页、历史记录或身份信息权限。

## 数据保留与删除

本扩展不会在开发者服务器保留任何数据。你可以删除同步文件、清空剪贴板或在 `chrome://extensions` 中清除本扩展数据。

## 安全提示

同步码使用密码加密。请通过可信渠道传递同步码，并不要把同步码和密码一起发送给不可信对象。忘记密码后，开发者无法恢复同步码内容。

## 政策变更与联系方式

如果本扩展未来改变数据处理方式，我们会更新本政策的生效日期和内容。公开发布时，请将本段替换为维护者的公开联系方式，例如 GitHub Issues 地址或专用邮箱。
