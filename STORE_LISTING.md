# Chrome Web Store 上架资料

## 基本信息

- 产品名：书签桥 · 加密书签同步
- 建议分类：生产力工具（Productivity）
- 版本：0.4.2
- 商店语言：简体中文
- 官网/项目主页：发布 GitHub 仓库后填写仓库地址
- 隐私政策 URL：将 `PRIVACY_POLICY.md` 发布为公开网页后填写

## 简短描述（132 字符以内）

用加密同步码在不同 Chrome 配置文件之间传递和合并书签，无需切换 Google 账号，不上传服务器。

## 详细描述

书签桥让 Chrome 书签迁移变得简单、可控、隐私友好。

当你需要把一个 Chrome 配置文件的书签带到另一个配置文件时：

1. 选择书签栏或其他书签位置。
2. 设置同步密码，生成加密同步码。
3. 复制同步码，或保存为 `.bookmarkbridge` 文件。
4. 在目标配置文件粘贴或导入文件，输入同一密码。
5. 预览内容后选择智能合并或全部添加。

产品特点：

- AES-GCM 加密同步码，密码不写入同步码。
- 本地处理，不上传书签、账号信息或浏览历史。
- 默认智能合并，复用同名文件夹并跳过重复网址。
- 支持文件导入，适合较大的书签树。
- 支持逐条同步差异预览、多次合并历史和分别撤销任意一次合并。
- 大书签树支持进度提示与取消操作；同步码过长时建议使用文件导入。
- 对大书签树进行压缩，并显示处理进度。
- 只申请书签和剪贴板写入权限。

重要说明：Chrome 扩展读取的是当前 Chrome 配置文件的书签，而不是某个 Google 账号云端书签。多个 Google 账号登录在同一配置文件时，它们共用该配置文件的书签集合。

## 截图清单

建议准备 5 张 1280×800 或 640×400 的浏览器截图，统一使用浅色界面并隐藏真实网址和个人账号信息：

1. **主界面**：显示“发送书签”和“接收并合并”两个区域。
2. **加密发送**：密码输入、生成加密同步码、复制/保存文件按钮。
3. **文件导入**：导入 `.bookmarkbridge` 文件后的状态。
4. **预览差异**：显示来源位置、文件夹/书签数量和目标位置选择。
5. **合并结果**：显示新增书签、文件夹和跳过重复项的结果提示。

截图注意：使用演示数据（如“阅读资料”“工作工具”），不要出现 Gmail 地址、真实书签网址、密码或同步码。

## 商店提交检查

- [ ] 上传 `icon-128.png` 作为商店图标。
- [ ] 准备 5 张截图，尺寸符合当前 Chrome Web Store 要求。
- [ ] 填写隐私政策公开 URL。
- [ ] 在权限说明中解释 `bookmarks`、`clipboardWrite` 的用途。
- [ ] 上传打包后的扩展 ZIP（不要包含 `assets/icon-source.png` 等源素材也可以，保留图标 PNG 即可）。
- [ ] 用全新 Chrome 配置文件安装 ZIP，完成一次发送、文件导入、错误密码和合并测试。
